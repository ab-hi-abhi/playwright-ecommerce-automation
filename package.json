{
  "name": "playwright-advantage",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:chromium": "npx playwright test --project=chromium",

    "test_Login" : "npx playwright test src/tests/login.spec.ts",
    "test_Checkout" : "npx playwright test src/tests/checkout.spec.ts",
    "test_Registartion" : "npx playwright test src/tests/auth.spec.ts",

    "test:All": "npx playwright test ",

    "install-browsers": "npx playwright install",
    "Show_report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^24.10.0",
    "typescript": "^5.0.0"
  }
}
